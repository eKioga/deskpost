# Local Shelf

Your local-only Books. They are never shared automatically.

A Book marked `**Kind:** capture` is a working Book rather than a curated one: `Add-ShelfNote.ps1`
may append raw notes to it, and its pages are captures that have not been checked since they were
written. Every other Book here is read-only in ordinary work.
